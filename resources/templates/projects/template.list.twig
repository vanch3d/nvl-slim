<li>
    {% spaceless %}<a href="{{ path_for("project.named",{'name':project.id}) }}" title="Go to the '{{ project.name }}' project page">
        {% block project_title %}{% endblock %}</a>{% endspaceless %}
    <p>{{ block('project_overview')|striptags|split(" ") |
            slice(0, highlight? 400 : 40 ) | join(" ") }} [...]</p>
</li>
